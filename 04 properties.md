###### properties
```css
#contents {
  width: calc(100% - var(--sidebar-width));
  height: calc(100vh - var(--header-height));
  padding: 20px;
  margin-top: var(--header-height); /* !!! very important */
  margin-left: var(--sidebar-width); /* !!! very important */
  
  display: flex;
  justify-content: space-between;
}
```
